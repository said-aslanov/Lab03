import streamlit as st

st.set_page_config(page_title="Phase 2 — API Analysis", page_icon="📊", layout="wide")
st.title("Phase 2 — API Analysis (Placeholder)")
st.write("This is a placeholder page for Phase 2. In this phase you will:")
st.markdown(
    """
    - Choose a **web API** (not the Pokémon API).
    - Provide **at least two user inputs** that change the app’s behavior.
    - Produce **at least one dynamic chart/visual** using API data.
    - Do your own processing of the fetched data (no LLM help here).
    """
)
st.info("Tip: keep this file name and location so links from the Home page work after deployment.")